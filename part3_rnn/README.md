# Part 3 — RNNs and Seq2Seq

This part implements and validates the RNN checklist.

Coverage:
- Probabilistic objective and chain rule: see `analysis/report.md` (generated by `validate_checklist.py`).
- Perplexity: small LM demo in `validate_checklist.py` (synthetic data).
- Models: `models.py` contains `SimpleRNN`, `SimpleLSTM`, `SimpleGRU`.
- Comparison: `validate_checklist.py` trains brief runs and outputs `analysis/models_comparison.json` and `figures/models_comparison.png`.
- BPTT and gradient clipping: `validate_checklist.py` plots `figures/grad_clip.png`.
- Tokenisation and padding: `train.py` shows simple vocab building and padding; `tokenizer.py` contains a basic tokenizer.
- Seq2Seq: `seq2seq.py` implements `SeqEncoder` and `SeqDecoder` with greedy and beam decoders; `validate_checklist.py` runs a copy-task demo and saves `analysis/seq2seq_examples.json`.

Run (light):

```bash
c:/python314/python.exe -c "import sys; sys.path.insert(0, r'C:\Users\lenovo\DL_Project\dl_multi_agents_project'); from part3_rnn.validate_checklist import run; print(run())"
```
